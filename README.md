# Workshop :: Spring Boot with Testing
* Spring Boot 4
* Postgresql
* Testing
  * JUnit 6
  * SpringBootTest
  * TestRestTemplate
  * TestContainers
* Docker
  * Docker Compose

## Start data

To start the PostgreSQL database using Docker Compose, run the following command:

```bash
$docker-compose up -d
$docker-compose ps
```